# Mendify: Autonomous DevOps Framework

Mendify is an autonomous DevOps framework that transforms CI/CD failures into automated recovery tasks. By integrating AI agents directly into GitHub Actions, it intercepts build errors, diagnoses root causes using an LLM reasoning engine, and orchestrates a Docker-in-Docker sandbox to safely develop and validate patches — pushing a corrective commit only once the build is verified green.
